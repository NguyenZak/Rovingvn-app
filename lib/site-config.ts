/**
 * Site Configuration - Main Export
 * Re-exports static site configuration for use throughout the application
 */

export { siteConfig, type SiteConfig } from "./site-config-static";
export { getDynamicSiteConfig, getCachedSiteSettings } from "./site-config-dynamic";
